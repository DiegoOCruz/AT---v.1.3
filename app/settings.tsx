import { AppBar, Grid, Text } from "@/components";

export default function SettingsScreen() {
  return (
    <>
      <Grid style={{
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center',
      }}>
        <Text>Configuraçoes</Text>
      </Grid>
    </>
  );
}
